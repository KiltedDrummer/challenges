function FirstReverse(str) { 

  // code goes here  
  testArr = str.split('');
  reversed = testArr.reverse();
  str = reversed.join('');
  return str; 
         
}
   
// keep this function call here 
FirstReverse(readline());
