function SimpleAdding(num) { 

  // code goes here  
  let product = 0;
  for (i = 0; i <= num; i++) {
      product += i;
  }
  num = product;
  return num; 
         
}
   
// keep this function call here 
SimpleAdding(readline());
